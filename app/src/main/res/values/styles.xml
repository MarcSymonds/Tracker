<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/textPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColor">#000000</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AboutBox" parent="AppTheme">
        <item name="android:background">#3333CC</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="AboutBox.Frame" parent="AboutBox">
        <item name="android:background">#ff9999ff</item>
    </style>

    <style name="MapToolbar" parent="AppTheme"></style>

    <style name="TrackedItemButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#FF9933FF</item>
        <item name="color">#FF3399FF</item>
    </style>
</resources>
